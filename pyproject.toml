[build-system]
requires = ["poetry-core>=1.0.0", "setuptools", "cython", "toml"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "McqGenerator"
version = "5.10.0"
description = ""
authors = ["AnushkaSonawane"]

[tool.poetry.dependencies]
python = ">=3.8,<3.10"
pandas = "1.5.3"
